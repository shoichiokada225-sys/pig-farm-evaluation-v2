# 記録用スプレッドシート連携（GAS）

- スプレッドシート: 「HSS実技試験V2 記録」 https://docs.google.com/spreadsheets/d/1Uym-B4iXMO8_H1y3dBww2d_AVgIrAkdbdgVPkOTH_dI/edit
- スクリプト: https://script.google.com/d/1kD1IRPzghY_JIwsaw787_gA6PqE6t_lLNaFlVzTbgFGhST_O0G0TNLi8/edit
- ウェブアプリURL（アプリの js/config.js に設定済み）:
  https://script.google.com/macros/s/AKfycbxFqIag1AwnT6WTaWlAf8Gb2Mh-JZRRB07DTrE6MAQLhyRIqZuwIsuMsWX-OaoxdvzP/exec
- オーナー: shoichi.okada225@gmail.com（clasp ログイン済み）

## 初回だけ（社長作業・2分）
1. 上のスクリプトURLを開く → 関数 `setup` を選んで ▶実行
2. 「権限を確認」→ アカウント選択 →「詳細」→「安全ではないページに移動」→ 許可
3. スプレッドシートに「受験者」「作業一覧」「農場一覧」タブができる。「受験者」に名簿を入れる（下の「シートの形」）

`setup` は何度実行してもよい。受験者タブの行と、農場一覧タブ（直した分）は消さない。作り直すのは作業一覧タブとプルダウンだけ。

## シートの形
### 受験者（名簿）— 見出しは1行目。記入の約束は「被評価者」の見出しのメモにもある
| 列 | 見出し | 書き方 |
|---|---|---|
| A | 農場 | プルダウン（農場一覧タブ）から選ぶ。農場が決まっていない人は **所属未確定** |
| B | 被評価者 | 1人1行。同じ農場に同じ名前の人がいる時は `Nam(A)` `Nam(B)` のように区別する |
| C〜J | 作業1〜作業8 | プルダウンの**作業名**から選ぶ（**No. は入らない**＝入力規則で拒否される）。空欄の人は（農場共通）行の作業 |
| K | 旧名 | 名前を直した時だけ、直す前の名前（複数は「、」区切り）。直す前の名前で保存した記録も「済」に数える |

旧名の列は見出しが「旧名」（または「別名」）なら位置は問わない。前の版の `setup` で作ったシートには無いので、使う時に見出し「旧名」の列を足す。

- **（農場共通）行**: 被評価者の欄に `（農場共通）` と書き、農場と作業を入れる。その農場で作業が空欄の人全員に使われる。1農場1行（2行あるとアプリが警告し、作業を合わせて使う）
- **所属未確定**: 後で農場を決めてよい。決めたら A列を書き換えて、アプリで「名簿を更新」。済んだ人は済んだまま（名簿で同じ名前が1人だけなら、名前で数える）
- **名前を直す時**（Nguyen Van A → Nguyễn Văn A など）: B列を直し、K列（旧名）に元の名前を書く
- **備考の列は自由に足してよい**（「作業メモ」など）。ただし見出しを「作業」+数字にしない（作業列として読まれ、プルダウンがかかる）
- 作業を 9つ以上にしたい時は、見出し `作業9` の列を足して `setup` を再実行
- 読むのは見出しで決めた列だけ。列の順番を入れ替えても読める

### 農場一覧
- A列=農場名（1行目は見出し）。最初の `setup` で、名簿の初期データ（無ければ受験者タブ）の農場＋所属未確定から作る
- 農場を増やす・名前を直す時はここを直す（受験者タブのプルダウンに反映）。`setup` を再実行しても上書きしない
- アプリは農場名の全角半角・空白のゆれを1つにまとめ、「大田原」と「大田原農場」のような似た名前の小さい農場は「農場名のゆれ」と警告する

### 作業一覧
- `setup` のたびに作り直す（アプリの作業カタログと同じ 40作業）。手で直さない

### 評価者名のタブ
- 評価者ごとに自動で作られる。1種目=1行。記録IDで上書きされるので、再送・編集しても重複しない
- **アプリで記録を削除すると、シートの行も消える**（どの評価者タブにあっても。圏外の時は電波が戻ってから）
- E列「農場」は**保存した時の農場**（所属未確定のまま採点した人は所属未確定）。後から直さない
- 評価者タブ自体に列を足さない（GAS が行を消して書き直すので、足した列の値がずれる）

## 農場別に集計する
評価者タブの「農場」は保存した時の値なので、所属が決まった人・農場名を直した人は、受験者タブの**今の農場**で数える。
別タブ（例「集計」）に評価者タブを並べ、今の農場の列を足す。**見出しは2行目から**（A1 には「記録ID」以外の文字を置く）:

```
A1: 集計（手で直さない）
A2: ={'評価者A'!A1:N; '評価者B'!A2:N}           ← 評価者タブを縦に並べる（タブ名は実際の名前に）
O2: 今の農場
O3: =IF(D3="",,XLOOKUP(D3,受験者!$B:$B,受験者!$A:$A,E3))   ← 下へコピー。名簿に無い名前は記録時の農場
```
GAS が行を消し・書き直すのは、**GAS 自身が作った評価者タブだけ**（タブの番号を文書プロパティ `HSS_EVAL_TAB_IDS` に控えている）。
集計タブ・タブの複製・値を貼った控えは、見出しが同じでも触らない。評価者タブを手で作ったり、控えを評価者名に改名したりしない。
同じ名前が2農場にいる人は `Nam(A)` のように区別しておく（XLOOKUP は最初の1人を返すため）。旧名で保存した記録は受験者タブのK列で引く:
`=XLOOKUP(D3,受験者!$B:$B,受験者!$A:$A,XLOOKUP("*"&D3&"*",受験者!$K:$K,受験者!$A:$A,E3,2))`

## コード更新（URLを変えない）
```
node gas/build_gas.js            # Code.src.gs → Code.gs / deploy/Code.js
node gas/test_gas.js             # 単体テスト
cd gas/deploy && clasp push --force && clasp update-deployment AKfycbxFqIag1AwnT6WTaWlAf8Gb2Mh-JZRRB07DTrE6MAQLhyRIqZuwIsuMsWX-OaoxdvzP
```
🚨 `clasp create-deployment` だと新URLになり、配布済みのアプリから届かなくなる。
